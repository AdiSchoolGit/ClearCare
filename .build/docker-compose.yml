services:
  nginx:
    build:
      context: .
      dockerfile: nginx.Dockerfile
    container_name: my-nginx-container
    ports:
      - "80:80"
    volumes:
      - ../.config/nginx.conf:/etc/nginx/nginx.conf
